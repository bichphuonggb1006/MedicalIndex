CREATE TABLE system_module(
                        id VARCHAR(50) PRIMARY KEY,
                        `version` VARCHAR(50),
                        `desc` TEXT,
                        attrs TEXT
);